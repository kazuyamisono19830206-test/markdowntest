name: Build Slides
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Marp
        run: npm install -g @marp-team/marp-cli
      
      - name: Convert to Slide PDF
        run: marp lesson.md --pdf -o slides.pdf
      
      - uses: actions/upload-artifact@v4
        with:
          name: slides
          path: slides.pdf
